import ReactDOM from 'react-dom/client';

import './index.css';       //we want inject css code into this JS file
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
